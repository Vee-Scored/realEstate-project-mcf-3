import React from 'react'

const SeeMoreBtn = ({className}) => {
  return (
    <button className={`w-[191px] h-[70px] bg-neutral-700 text-white ${className} `}>
        See More
    </button>
  )
}

export default SeeMoreBtn